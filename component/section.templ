package component

import (
	sitter "github.com/smacker/go-tree-sitter"
)

templ Section(node *sitter.Node, lines *[]string, c *context.Context) {
	for i := 0; i < int(node.NamedChildCount()); i++ {
		@Parser(node.NamedChild(i), lines, c)
	}
}
